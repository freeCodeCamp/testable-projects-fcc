/**
 * Mocha setup example.
 *
 * Copy this file to setup.js and change as needed.
 *
 */

// What browsers are used for testing
global.browsers = [
//  'chrome',
//  'firefox'
];

// Location of chrome binary. Uncomment one of the below. The first is for
// Ubuntu (might work for other Linux flavors). The second is for Mac. Please
// feel free to add other examples here if you find something that works for
// another platform.
// global.chromeBinaryPath = '/usr/bin/google-chrome-stable';
// global.chromeBinaryPath =
//     '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome';
// global.chromeBinaryPath =
//   'C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe';

// Location of firefox binary. Uncomment one of the below. The first is for
// Ubuntu (might work for other Linux flavors). Please
// feel free to add other examples here if you find something that works for
// another platform.
// global.firefoxBinaryPath = '/usr/bin/firefox';
// global.firefoxBinaryPath =
//   'C:\\Program Files\\Mozilla Firefox\\firefox.exe';

// The URL you want to be used for the bundle.js when running local tests.
// The below value is typically used when you are using:
// "yarn develop".
global.bundleUrl = 'https://127.0.0.1:8080/bundle.js';

// Where to save screenshots.
global.screenshotDir = 'test/screenshots';

// Specify your maximum screen size.
// If you are on a Mac (and now it seems like headless Chrome also has this
// problem) the Chrome argument "--start-maximized" is ignored. So we just set
// the screen size manually.
global.browserMaxWidth = 1920;
global.browserMaxHeight = 1080;
